"use client";
export default function Reports() {
	// try {
	// 	const officeName = localStorage.getItem("officeName");
	// 	const socket = new WebSocket(
	// 		`wss://8igcbtgefa.execute-api.ap-south-1.amazonaws.com/prod?office_name=${"Nagpur"}`
	// 	);
	// 	socket.addEventListener("open", () => {
	// 		console.log("WebSocket connection established");
	// 	});
	// 	socket.addEventListener("message", (event) => {
	// 		console.log("A MESSGE IS RECEIVED : ", event.data);
	// 	});
	// } catch (error) {}
	return <section>Report SECTION</section>;
}
